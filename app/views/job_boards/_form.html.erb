<%= form_for(job_board) do |f| %>
  <% if job_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_board.errors.count, "error") %> prohibited this job_board from being saved:</h2>

      <ul>
      <% job_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %>
    <%= f.number_field :level %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
